<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.RecDeckApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Splash screen customizations for Android 12+ only -->
        <item name="android:windowSplashScreenBackground">@color/primary_main</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_app_logo_splash_12</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/primary_main</item>
        <item name="postSplashScreenTheme">@style/Theme.RecDeckApp</item>

    </style>
</resources>
